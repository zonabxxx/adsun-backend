# Environment
NODE_ENV=production

# Server
PORT=3001

# MongoDB
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/ADsun
MONGODB_DB_NAME=ADsun

# JWT
JWT_SECRET=REPLACE_WITH_SECURE_SECRET
JWT_EXPIRY=15m
REFRESH_TOKEN_SECRET=REPLACE_WITH_SECURE_SECRET
REFRESH_TOKEN_EXPIRY=7d

# Frontend URL (pre CORS nastavenia)
FRONTEND_URL=https://www.edon.sk

# Logging
LOG_LEVEL=info

# Instructions:
# 1. Copy this file to .env
# 2. Replace the JWT_SECRET and REFRESH_TOKEN_SECRET with secure random strings
# 3. Update MONGODB_URI with your actual database connection string
# 4. Never commit the actual .env file to version control 